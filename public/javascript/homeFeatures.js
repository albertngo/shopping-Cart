//add to cart events
let addCart = document.querySelectorAll(".addCart");

for (let button of Array.from(addCart)){
    button.addEventListener("click",()=>{
        
        //get all data neccessary
        //imgsrc
        let imgSrc = button.parentElement.children[0].getAttribute("src");
        //h1
        let prodName = button.parentElement.children[]
        //price

        //put into an object
        
        //send as fetch request

        //modal to say it was added
    })
    //fire post event


}
